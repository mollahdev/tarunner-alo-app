import styled from '@emotion/native';

export const FlexWrapper = styled.View`
    column-gap: 10px;
    row-gap: 20px;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
`;
